{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development ts-node -r tsconfig-paths/register index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
